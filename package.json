{
  "name": "openexpense.org",
  "private": true,
  "version": "2.1.0",
  "scripts": {
    "prebuild": "node scripts/clean-build-assets.mjs",
    "build": "npx esbuild src/main.js --bundle --format=esm --minify --target=es2020 --splitting --outdir=. --entry-names=app --chunk-names=chunk-[name]-[hash]",
    "postbuild": "node scripts/check-bundle.mjs",
    "validate": "npm run build",
    "serve": "python3 -m http.server 8765"
  },
  "devDependencies": {
    "esbuild": "^0.25.0"
  },
  "dependencies": {
    "fflate": "^0.8.3",
    "jspdf": "^4.2.1",
    "jspdf-autotable": "^5.0.8"
  }
}
