{
  "name": "openexpense.org",
  "private": true,
  "scripts": {
    "build": "npx esbuild src/main.js --bundle --format=esm --minify --target=es2020 --outfile=app.js",
    "serve": "python3 -m http.server 8765"
  },
  "devDependencies": {
    "esbuild": "^0.25.0"
  }
}
