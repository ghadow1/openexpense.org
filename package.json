{
  "name": "openexpense.org",
  "private": true,
  "scripts": {
    "build": "esbuild src/main.js --bundle --format=esm --outfile=app.js",
    "serve": "python3 -m http.server 8765"
  },
  "devDependencies": {
    "esbuild": "^0.25.0"
  }
}
